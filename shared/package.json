{
  "name": "dupe-more-shared",
  "version": "0.1.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "build": "echo 'No build needed for shared library'",
    "dev": "echo 'Shared library ready'",
    "lint": "echo 'No lint for shared library'"
  },
  "dependencies": {
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  }
}